annotated-types==0.8.0
setuptools==70.3.0
anyio==4.14.2
certifi==2026.7.22
click==8.1.8
colorama==0.4.6
deprecation==2.1.0
dnspython==2.8.0
dotenv==0.9.9
email-validator==2.3.0
fastapi==0.115.0
gotrue==2.12.4
h11==0.16.0
h2==4.4.1
hpack==4.2.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.27.2
hyperframe==6.1.0
idna==3.19
livekit-api==1.2.0
packaging==26.3
postgrest==0.16.11
pydantic==2.12.2
PyJWT==2.13.0
python-dateutil==2.9.0.post0
python-dotenv==1.0.1
python-multipart==0.0.32
PyYAML==6.0.3
realtime==2.5.3
six==1.17.0
sniffio==1.3.1
starlette==0.38.6
storage3==0.7.7
StrEnum==0.4.15
supabase==2.7.4
supafunc==0.5.1
typing_extensions==4.16.0
uvicorn==0.30.6
watchfiles==1.2.0
websockets==15.0.1
razorpay==1.4.2
google-auth==2.57.0
tenacity==9.1.4
# SpeechRecognition/gTTS/pydub below are leftovers from the old orphaned
# app/voice_agent.py flat file (dead code, not used by voice_routes.py or
# the current app/voice_agent/ package) - harmless to keep, safe to remove
# once that legacy file is deleted.
SpeechRecognition==3.10.4
gTTS==2.5.1
pydub==0.25.1

# google-genai is installed SEPARATELY via:
#   pip install --no-deps google-genai==1.23.0
# NOT here in requirements.txt - including it in this list forces pip to
# resolve its httpx>=0.28.1 requirement against the httpx==0.27.2 pinned
# above (needed by the already-installed supabase stack), causing a
# ResolutionImpossible conflict. --no-deps installs google-genai's own code
# without touching httpx or websockets at all - safe here since app/agents/
# only calls the plain REST generate_content() API, never the
# websocket-based Live/streaming API, so the version mismatch never
# actually matters at runtime.
#
# Render Build Command should be:
#   pip install -r requirements.txt && pip install --no-deps google-genai==1.23.0
#
# livekit-api (above) is enough for voice_routes.py's token minting
# (`from livekit import api`) - it does NOT need the full livekit-agents /
# livekit-plugins-* stack, those belong only to the separate voice worker
# service (see requirements-voice-agent.txt).